export default function CatalogoPage() {
  return (
    <main className="px-6 py-16">
      <h1 className="text-3xl font-bold mb-4">Catalogo</h1>
      <p className="text-black/70 max-w-2xl">
        TODO: listar productos de stock (camisetas y buzos ya estampados o
        listos para bordar) desde la tabla `products` de Supabase
        (is_published = true), con checkout en linea via la pasarela de
        pagos definida.
      </p>
    </main>
  );
}
