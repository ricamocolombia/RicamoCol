export default function InventarioPage() {
  return (
    <main className="px-6 py-10">
      <h1 className="text-2xl font-bold mb-2">Inventario</h1>
      <p className="text-neutral-600 max-w-2xl">
        TODO: stock de prendas en blanco (camisetas, buzos) por talla y
        color, tablas `inventory_items` + `inventory_movements`, alertas de
        stock bajo (reorder_level).
      </p>
    </main>
  );
}
